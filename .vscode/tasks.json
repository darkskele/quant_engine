{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "CMake: configure and build",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "[[ -f build/CMakeCache.txt ]] || (mkdir -p build && cd build && cmake ..); cd build && cmake --build . -j"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Configure with CMake only if not already configured, then build"
    }
  ]
}
